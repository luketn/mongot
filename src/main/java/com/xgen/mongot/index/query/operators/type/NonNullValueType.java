package com.xgen.mongot.index.query.operators.type;

/** Non-null value types supported in query operators. */
public enum NonNullValueType {
  BOOLEAN,
  OBJECT_ID,
  NUMBER,
  DATE,
  STRING,
  UUID
}
