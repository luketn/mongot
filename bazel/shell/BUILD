# Shell rules configuration for Bazel 8.0.1+
# Note: Bazel 8.0.1+ includes built-in forwarding from @bazel_tools//tools/sh:toolchain_type
# to rules_shell (see #24793), so no custom compatibility toolchains are needed.
